# Trainer frontend — local Vite (public vars only; never put secrets in VITE_*).
# Copy: cp .env.example .env

# App
VITE_APP_ENV=development
VITE_PUBLIC_ORIGIN=https://localhost

# API is same-origin `/api` (FR-005a) — no VITE_API_URL in F1.
